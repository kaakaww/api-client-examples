# StackHawk Sample API Client

This uses the Maven OpenAPI Generator to build model classes and associated clients for interacting with the
StackHawk API.

## To Run:

```shell
> mvn -e clean generate-sources compile exec:java  -Dexec.args="
```